{# empty Twig template #}
{% extends 'BaseLayout.template.twig' %}
{% block content %}
    <div class='container'>
    <h1>Registreer</h1>
    <div class='col-sm-4 col-sm-offset-4 loginform'>
        {% if login %}
            <p>Bedankt {{ login.voornaam }} {{ login.naam }} om te registreren. </p>
            Klik <a href="index.php">hier</a> om terug te keren naar de homepagina.
            {% else %}
                {% include 'form.html.twig' %}
        {% endif %}
    </div>
    </div>
{% endblock %}