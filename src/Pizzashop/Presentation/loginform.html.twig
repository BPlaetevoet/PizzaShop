{# loginform.html.twig #}
<form role="login" method="post" action="login.php?actie=inloggen">
        <div class="form-group">
                <label for="mail">Email</label>
                <input type="mail" class="form-control" name="mail" id="mail" placeholder="e-mail" {% if cookie %}value="{{ cookie }}{% endif %}"/>
            </div>
            <div class="form-group">
                <label for="password">wachtwoord</label>
                <input type="password" class="form-control" name="password" id="password" placeholder="wachtwoord"/>
            </div>
            <button type="submit" class="btn pizza-btn">Inloggen</button>
    </form>